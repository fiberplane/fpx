---
import autoDetectedRoutesDark from "@/assets/bento-auto-detected-routes-dark.jpg?url";
import autoDetectedRoutesLight from "@/assets/bento-auto-detected-routes-light.jpg?url";
import tracesDark from "@/assets/bento-traces-dark.jpg?url";
import tracesLight from "@/assets/bento-traces-light.jpg?url";
---

<section class="container not-content">
  <div class="bento-grid">
    <article class="bento-grid-item">
      <h3>Other feature</h3>
      <p>
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Quod nam, non
        accusamus ex iure cupiditate.
      </p>
    </article>

    <article class="bento-grid-item bento-grid-item--horizontal">
      <div>
        <h3>Auto detected routes</h3>
        <p>
          Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quasi saepe
          dignissimos error recusandae non? Quisquam officiis velit voluptatem
          nam ab blanditiis. Harum, eligendi. Quisquam odio numquam optio unde
          necessitatibus distinctio.
        </p>
      </div>

      <div class="bento-grid-item-image">
        <img
          class="light:sl-hidden"
          src={autoDetectedRoutesDark}
          alt="Screenshot of auto detected routes in FPX Studio"
        />
        <img
          class="dark:sl-hidden"
          src={autoDetectedRoutesLight}
          alt="Screenshot of auto detected routes in FPX Studio"
        />
      </div>
    </article>

    <article class="bento-grid-item">
      <h3>Traces</h3>
      <p>Lorem ipsum dolor sit amet consectetur, adipisicing elit?</p>

      <div class="bento-grid-item-image">
        <img
          class="light:sl-hidden"
          src={tracesDark}
          alt="Screenshot of auto detected routes in FPX Studio"
        />
        <img
          class="dark:sl-hidden"
          src={tracesLight}
          alt="Screenshot of auto detected routes in FPX Studio"
        />
      </div>
    </article>

    <article class="bento-grid-item">
      <h3>Automatically generated routes</h3>
      <p>
        Lorem ipsum, dolor sit amet consectetur adipisicing elit. Magnam
        dolorum, illum, consequuntur.
      </p>

      <div class="icon-container">✨</div>
    </article>
  </div>
</section>

<style>
  .container {
    --bento-item-radius: 1rem;
    --bento-item-padding: 2.5rem;

    container: bento-container / inline-size;
    background-color: var(--bg-secondary);
    margin-inline: calc(var(--sl-content-pad-x) * -1);
    margin-block: 4rem !important;
    padding-block: 6rem;
  }

  .bento-grid {
    display: grid;
    grid-template: auto auto / repeat(5, 1fr);
    gap: 1.5rem;
    max-width: 72rem;
    margin-inline: auto;
  }

  .bento-grid-item {
    grid-column: span 3;

    border-radius: var(--bento-item-radius);
    border: 1px solid var(--sl-color-hairline-shade);

    padding: var(--bento-item-padding);

    background-color: var(--sl-color-bg);
    background-image: radial-gradient(
      circle at bottom center,
      var(--bg-secondary),
      rgb(from var(--sl-color-bg) r g b / 0.05)
    );

    overflow: hidden;

    &:nth-child(4n + 1),
    &:nth-child(4n) {
      grid-column: span 2;
    }
  }

  .bento-grid-item h3 {
    font-size: 32px;
    line-height: var(--sl-line-height-headings);
    margin-bottom: 0.65em;
  }

  .bento-grid-item--horizontal {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: repeat(2, 1fr);
  }

  .bento-grid-item-image {
    overflow: hidden;
    position: relative;
    min-height: 100%;
    width: calc(100% + var(--bento-item-padding));
    margin-bottom: calc(var(--bento-item-padding) * -1);

    border-top-left-radius: var(--border-radius);

    border-style: solid;
    border-color: var(--sl-color-hairline-shade);
    border-width: 1px;
    border-right: none;
    border-bottom: none;

    box-shadow: 0px 6px 24px 0px var(--bg-emphasis);

    img {
      position: absolute;
      display: block;
      max-width: 100%;
      height: auto;
    }
  }

  .icon-container {
    display: grid;
    justify-content: center;
    font-size: 6rem;
    filter: hue-rotate(320deg) contrast(120%);
  }
</style>
